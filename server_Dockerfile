FROM golang

WORKDIR /server

ARG HOST
ENV HOST=$HOST
ARG PORT
ENV PORT=$PORT
ARG DB
ENV DB=$DB
ARG USER
ENV USER=$USER
ARG PASS
ENV PASS=$PASS
ARG TIMEOUT
ENV TIMEOUT=$TIMEOUT
COPY ./src .
EXPOSE 8000
CMD ["go", "run", "main.go"]

